CREATE DATABASE MessageLogs;

CREATE TABLE Logs (
userID VARCHAR(2000),
username VARCHAR(2000),
textDisc VARCHAR(2000),
timeJs VARCHAR(2000)
);

/* SELECT * FROM table WHERE column = 'string'; */